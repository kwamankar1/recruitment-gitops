apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ui.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.ui.serviceType }}
  selector:
    app: {{ .Values.ui.name }}
  ports:
  - port: 80
    targetPort: {{ .Values.ui.port }}
